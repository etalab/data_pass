<%= form_with(model: @message, url: ) do |f| %>
  <%= f.dsfr_text_area :body, placeholder: t(".placeholder"), required: true %>
  <%= f.button t(".cta"),
           type: :submit,
           class:
             "fr-btn fr-btn--primary fr-btn--icon-left fr-icon-send-plane-line" %>
<% end %>
