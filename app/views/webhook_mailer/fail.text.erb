<%= render partial: 'mailer/shared/applicant/header', locals: { entity_name: nil } %>

<%=
  t(
    '.description',
    authorization_definition_name: @authorization_definition_name,
    webhook_url: @webhook_url,
    webhook_calls_url: @webhook_calls_url
  )
%>

<%= t('mailer.shared.instruction.footer.text') %>
