Bonjour,

Une <%= @authorization_request.reopening? ? 'mise à jour' : 'habilitation' %> <%= @authorization_request.definition.name_with_stage %> pour le FS <%= @authorization_request.organization.name %> vient d'être validée.
Identifiant DataPass de l'habilitation FranceConnect associée : <%= authorization_request_url(@linked_authorization.request) %> (<%= @linked_authorization.formatted_id %>).

En conséquence, merci d'autoriser les scopes suivants pour le sus-mentionné FS :

<% @authorization_request.scopes.each do |scope| %>
- <%= scope %>
<% end %>

Pour consulter cette <%= @authorization_request.reopening? ? 'mise à jour' : 'habilitation' %>, veuillez cliquer sur le lien suivant <%= authorization_request_url(@authorization_request) %> .

L'équipe DataPass
