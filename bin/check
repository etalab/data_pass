#!/bin/bash

set -e
trap "echo -ne \"\n\n❌ Something went wrong\n\"" ERR

bundle exec rubocop -F &&
  bundle exec rspec --fail-fast &&
  bundle exec cucumber --fail-fast &&
  echo -ne "\n\n✅ All good"
